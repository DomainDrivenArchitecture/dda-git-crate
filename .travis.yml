sudo: required

language: clojure

services:
  - docker

jobs:
  include:
    - stage: build
      script:
        - lein version
        - lein deps
        - lein test
        - lein uberjar
    - stage: integrationtest
      script:
        - docker build -t dda/dda-git-crate .
